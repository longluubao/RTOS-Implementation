# Core Folder Logic

This `Core` folder contains the fundamental logical components and source code for the RTOS-Scheduler project.

## Overview
The files within this directory are responsible for:
- Initializing the microcontroller.
- Managing tasks and their execution.
- Handling system-level interrupts and exceptions.
- Implementing core RTOS functionalities such as context switching, scheduling algorithms, and inter-task communication mechanisms.

## Key Components (placeholder - replace with actual details)
- `main.c`: The entry point of the application, responsible for system setup and starting the RTOS kernel.
- `stm32f1xx_it.c`: Contains interrupt service routines (ISRs) for various peripherals and system exceptions.
- `syscalls.c`: Implements system calls for interacting with the RTOS kernel.
- `[RTOS_Kernel_Files]`: Files related to the RTOS kernel implementation (e.g., task management, scheduler, synchronization primitives).

## How to add more details:
Please provide specific details about the files and their functionalities within this `Core` folder, and I can update this README accordingly.
